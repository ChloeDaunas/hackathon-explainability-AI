# 🛡️ TalentGuard AI — Hackathon Trusted AI x HR

> **Capgemini x ESILV** · March 16-17, 2026  
> A responsible AI solution to prevent employee turnover

---

## 🎯 Objective

An imaginary company is facing a high resignation rate. **TalentGuard AI** is a solution that helps HR management:

- **Identify** employees at risk of resignation
- **Understand** the explanatory factors (no black box)
- **Act** ethically, transparently and in compliance with GDPR

The project covers **two Trusted AI axes**:

| Axis | What we do |
|------|------------|
| 🔒 **Cybersecurity** | GDPR anonymization, EU AI Act classification, exclusion of sensitive attributes |
| ⚖️ **Ethics** | Fairness audit (gender, ethnicity), SHAP explainability, AI Act compliance |

---

## 👤 Personas & Use Case

**Main Use Case**: *Analysis and prevention of resignation risk*

**Persona 1 — HR Manager**
> *"I need to know which employees are at risk of leaving, why, and how to act — without discriminating."*

**Persona 2 — Management / CTO**
> *"I want a reliable, GDPR-compliant and explainable AI solution for my teams."*

---

## 🗂️ Project Architecture

```
talentguard-ai/
├── app.py                              # Streamlit application (demo)
├── requirements.txt                    # Python dependencies
├── README.md
│
├── data/
│   ├── HRDataset_v14.csv               # Original dataset (Kaggle - Rich Huebner)
│   ├── HRDataset_v14_enriched.csv      # Dataset with NLP text columns
│   ├── HR_Dataset_Anonymized.csv       # Generated by anonymize_data.py
│   ├── HR_NLP_scores.csv               # Generated by nlp_pipeline.py
│   ├── fairness_audit_results.csv      # Generated by fairness_xai.py
│   ├── shap_feature_importance.csv     # Generated by fairness_xai.py
│   ├── shap_global.png                 # Generated by fairness_xai.py
│   └── shap_local.png                  # Generated by fairness_xai.py
│
├── models/
│   ├── rf_model.joblib                 # Trained RF model (generated by train_model.py)
│   ├── nlp_model.joblib                # Trained NLP model (generated by nlp_pipeline.py)
│   └── model_feature_columns.json     # Model feature columns
│
├── docs/
│   ├── MODEL_CARD_RF_EN.docx           # Model card — Random Forest
│   └── MODEL_CARD_NLP_EN.docx          # Model card — NLP TF-IDF + LogReg
│
└── src/
    ├── anonymize_data.py               # Step 1: GDPR anonymization
    ├── train_model.py                  # Step 2: Random Forest training
    ├── fairness_xai.py                 # Step 3: fairness audit + SHAP
    └── nlp_pipeline.py                 # Step 4: NLP sentiment + risk score
```

---

## ⚙️ Installation

**Requirements**: Python 3.10+

```bash
# 1. Clone the repo
git clone <REPO_URL>
cd talentguard-ai

# 2. Create and activate virtual environment
python -m venv venv
source venv/bin/activate        # Linux/Mac
venv\Scripts\activate           # Windows

# 3. Install dependencies
pip install -r requirements.txt
```

---

## 🚀 Running the Pipeline

Execute the scripts in order:

```bash
# Step 1 — GDPR data anonymization (Cybersecurity axis)
python src/anonymize_data.py

# Step 2 — ML model training
python src/train_model.py

# Step 3 — Fairness audit + SHAP (Ethics axis)
python src/fairness_xai.py

# Step 4 — NLP sentiment analysis
python src/nlp_pipeline.py

# Step 5 — Launch the application
streamlit run app.py
```

The application is available at `http://localhost:8501`

---

## 📊 Data

**Source**: [Human Resources Dataset — Kaggle (Rich Huebner & Carla Patalano)](https://www.kaggle.com/datasets/rhuebner/human-resources-data-set)

- ~311 employees (after anonymization)
- Target variable: `Termd` (0 = active, 1 = left)
- Turnover rate: ~33%
- Sensitive attributes present: `Sex`, `RaceDesc`, `GenderID`

**Data Card — Transformations applied:**

| Technique | Columns affected | Justification |
|-----------|-----------------|---------------|
| Suppression | `Employee_Name`, `ManagerName` | Direct identifiers |
| Pseudonymization | `EmpID` | Truncated SHA-256 hash, non-reversible |
| Generalization | `DOB` → `AgeGroup` | Useful info without exact date |
| Masking | `Zip` → first 2 digits | Reduction of geographic precision |
| Model exclusion | `Sex`, `RaceDesc`, `GenderID` | Retained only for fairness audit |

---

## 🤖 Model Cards

Two models are used in TalentGuard AI — full model cards available in `/docs/`.

### Random Forest (structured data)

| Parameter | Value |
|-----------|-------|
| Algorithm | Random Forest Classifier |
| Library | scikit-learn 1.5.0 |
| `n_estimators` | 100 |
| `class_weight` | balanced (imbalanced classes) |
| Split | 80% train / 20% test (stratified) |
| Features | 133 columns after one-hot encoding |
| **Precision (Left)** | **0.72** |
| **Recall (Left)** | **0.62** |
| **F1-score (Left)** | **0.67** |
| **Accuracy** | **0.79** |

**Top features (SHAP)**: `TenureYears`, `Salary`, `EngagementSurvey`, `Absences`, `EmpSatisfaction`

> Recall is the priority metric: missing a true resignation is more costly than a false alert.

### NLP Model (text data)

| Parameter | Value |
|-----------|-------|
| Algorithm | TF-IDF + Logistic Regression |
| Library | scikit-learn 1.5.0 |
| Vocabulary size | 500 features (ngrams 1-2) |
| `class_weight` | balanced |
| **Recall (Left)** | **1.00** ⚠️ synthetic data |
| **F1-score (Left)** | **0.95** |
| **Accuracy** | **0.97** |

> High NLP performance is due to the synthetic nature of the texts (AI-generated, aligned with labels). Lower performance expected on real data.

---

## ⚖️ Fairness Audit

Metrics computed using AIF360 (IBM) methods on the test set (63 employees):

| Attribute | Disparate Impact | Dem. Parity Diff | Eq. Odds Diff | Verdict |
|-----------|-----------------|-----------------|----------------|---------|
| Gender (M vs F) | 1.066 | +0.018 | 0.155 | ✅ DI OK · ⚠️ EOD to monitor |
| Ethnicity (White vs others) | 0.826 | -0.054 | 0.155 | ✅ DI OK · ⚠️ EOD to monitor |

**Interpretation rules:**
- Disparate Impact: acceptable between **0.8 and 1.25** (80% rule)
- Demographic Parity Diff: ideal = **0**, alert threshold = **±0.1**
- Equalized Odds Diff: ideal = **0**, alert threshold = **0.1**

---

## 🇪🇺 EU AI Act Classification

This system is classified as **HIGH RISK** (Annex III — HR and employment management systems).

**Obligations implemented in this project:**
- ✅ Mandatory human oversight (recommendations, no automated decisions)
- ✅ Prediction traceability (scores saved)
- ✅ Documented fairness audit (`fairness_audit_results.csv`)
- ✅ Right to explanation (SHAP waterfall plot per employee in the app)

---

## 🖥️ Application — Pages

| Page | Content |
|------|---------|
| 📊 **Dashboard** | Global KPIs, risk score distribution, RF vs NLP comparison, top 10 at-risk employees |
| 🔍 **Individual Analysis** | Score per employee, SHAP explanation, automatic HR recommendation |
| ⚖️ **Fairness Audit** | Metrics by group (gender/ethnicity), AIF360 validation, AI Act classification |
| 🔐 **Security** | Input validation demo, attack simulation, validation rules table |
| ℹ️ **About** | Tech stack, project summary |

---

## 🛠️ Tech Stack

```
Python 3.10
├── pandas          → data manipulation
├── scikit-learn    → ML models (Random Forest, TF-IDF + LogReg)
├── shap            → explainability (SHAP values, waterfall plots)
├── aif360          → fairness audit (IBM)
├── fairlearn       → complementary fairness metrics
├── lime            → alternative local explanation method
├── streamlit       → web application
└── matplotlib      → visualizations
```

---


